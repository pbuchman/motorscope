{
  "name": "motorscope",
  "version": "1.0.0",
  "description": "MotorScope - Car Listing Tracker monorepo containing Chrome extension and backend API",
  "private": true,
  "workspaces": [
    "extension",
    "api"
  ],
  "scripts": {
    "build": "npm run build:extension && npm run build:api",
    "build:extension": "cd extension && npm run build",
    "build:api": "cd api && npm run build",
    "dev:extension": "cd extension && npm run dev",
    "dev:api": "cd api && npm run dev",
    "start:api": "cd api && npm start",
    "install:all": "npm install && cd extension && npm install && cd ../api && npm install",
    "clean": "rm -rf extension/dist api/dist"
  }
}
