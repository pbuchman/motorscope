# =============================================================================
# MotorScope - Environment Variables Reference
# =============================================================================
#
# This file documents ALL environment variables used across the project.
# It is NOT loaded by any application - it serves as documentation only.
#
# For actual configuration:
#   - API: Copy api/.env.example to api/.env
#   - Extension: Settings are configured in the extension UI
#
# =============================================================================

# =============================================================================
# API ENVIRONMENT VARIABLES (api/.env)
# =============================================================================
# These variables are used by the backend API.
# Copy api/.env.example to api/.env for local development.

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# NODE_ENV: Application environment
# Values: "development" | "production"
# Default: "development"
NODE_ENV=development

# PORT: HTTP server port
# Cloud Run automatically sets this in production
# Default: 8080
PORT=8080

# -----------------------------------------------------------------------------
# GCP Configuration
# -----------------------------------------------------------------------------
# GCP_PROJECT_ID: Google Cloud Project ID
# Used for Firestore and other GCP services
GCP_PROJECT_ID=motorscope

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# JWT_SECRET: Secret key for signing JWT tokens
# REQUIRED in production. Generate with: openssl rand -base64 32
# Security: Never commit actual secrets to git
JWT_SECRET=

# OAUTH_CLIENT_ID: Google OAuth 2.0 Client ID
# From GCP Console > APIs & Services > Credentials
# Used to verify Google ID tokens from the Chrome extension
OAUTH_CLIENT_ID=663051224718-nj03sld1761g1oicnngk1umj0ob717qe.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
# ALLOWED_ORIGIN_EXTENSION: Chrome extension origin for CORS
# Format: chrome-extension://<extension-id>
# Find your extension ID in chrome://extensions after loading unpacked
ALLOWED_ORIGIN_EXTENSION=chrome-extension://your-extension-id

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# BACKEND_BASE_URL: Public URL of this API (for self-reference in responses)
# Set automatically by Cloud Run, only needed for special cases
# BACKEND_BASE_URL=https://motorscope-api-xxxxx.europe-west1.run.app/api


# =============================================================================
# EXTENSION CONFIGURATION (configured in UI)
# =============================================================================
# These are NOT environment variables - they are configured in the extension
# settings page and stored in chrome.storage.local or the remote backend.

# Backend Server URL (configured in Settings page)
# Options:
#   - Cloud: https://motorscope-api-663051224718.europe-west1.run.app/api
#   - Local: http://localhost:8080/api

# Gemini API Key (configured in Settings page)
# Get from: https://aistudio.google.com/app/apikey

# Check Frequency (configured in Settings page)
# How often to refresh listings (10 seconds to 1 month)


# =============================================================================
# CLOUD RUN DEPLOYMENT
# =============================================================================
# When deploying to Cloud Run, set these environment variables in the
# Cloud Run service configuration (Console or gcloud CLI):
#
# Required:
#   NODE_ENV=production
#   GCP_PROJECT_ID=motorscope
#   JWT_SECRET=<your-secret>  (use Secret Manager for production)
#   OAUTH_CLIENT_ID=663051224718-xxx.apps.googleusercontent.com
#   ALLOWED_ORIGIN_EXTENSION=chrome-extension://<your-extension-id>
#
# See BACKEND.md for detailed deployment instructions.


# =============================================================================
# GCP INFRASTRUCTURE REFERENCE
# =============================================================================
# These are infrastructure values, not environment variables.
# Documented here for reference.

# Cloud Run Service: motorscope-api
# Cloud Run Region: europe-west1
# Firestore Database ID: motorscopedb
# Firestore Location: europe-west1

